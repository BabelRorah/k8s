replicaCount: 2

image:
  repository: BabelRorah/k8s_eval
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

env:
  RESPONSE: "it works!"

resources: {}

nodeSelector: {}
tolerations: []
affinity: []